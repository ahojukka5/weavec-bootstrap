# Build outputs (recreated by ./build.sh on every run).
# Includes build/vendor/{weavec0,weavec1} where the dependencies are
# auto-fetched.
build/

# LLVM intermediates left around outside build/ for any reason.
*.bc
*.o

# Editor / OS noise.
.DS_Store
*.swp
*.swo
*~

# Generated local state. Not source.
Manifest.toml
Manifest-*.toml
LocalPreferences.toml
*.mem
.pytest_cache/
.quarto/
_build/
runs/
*.out
*.err
run.log
inst.log
